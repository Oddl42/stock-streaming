apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-ui.fullname" . }}-web
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "stock-ui.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
